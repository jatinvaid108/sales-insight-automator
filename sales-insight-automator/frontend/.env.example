# ─── Backend API URL ─────────────────────────────────────────────────────────
# Leave empty for local development (Vite proxy handles /api → localhost:5000)
# Set to your Render backend URL in production:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
